source 'https://rubygems.org'

gem 'aws-sdk-kms'

gem 'nypl_log_formatter'
gem 'nypl_platform_api_client', '~> 1.0'
# Note thee appears to be a regression in v1.3.0
# Empty 201 responses are erroroneously handled as errors:
gem 'nypl_sierra_api_client', '=1.1.0'
gem 'sendgrid-ruby', '>=6.6.2'
gem 'logger'
gem 'ostruct'
gem 'rexml'

group :test do
  gem 'rspec'
  gem 'webmock'
  gem 'dotenv'
end

